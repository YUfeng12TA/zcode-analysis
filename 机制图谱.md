# ZCode 机制图谱（全量破解）

> 全部锚点可用 grep 复核。引擎 = backend/zcode.cjs（3583 行原版压缩）；
> 已解混淆全量产物在 deobfuscated/（main 20 + host 15 + preload 6 + scheduler 1 + renderer 2686）。

## 机制命中统计（zcode.cjs 引擎内 grep 计数）

| 机制 | 关键词 | 命中次数 | 说明 |
|---|---|---|---|
| 模型调用网关 | getModelConfigHeaders / o11yHeaders | 11 / 11 | 每处模型请求注入可观测头 |
| 工具调度 | toolCall / toolName / startTool / runTool | 1238 / 882 / 7 / 4 | 工具调用是引擎核心循环 |
| 遥测可观测 | opentelemetry / telemetry / o11y / arms | 606 / 153 / 16 / 9 | OpenTelemetry + 阿里 ARMS |
| MCP 协议 | callTool / listTools | 6 | @modelcontextprotocol/sdk |
| computer-use | computerUse / cua_ / playwright | 15 | 浏览器自动化 |
| 仓库快照 | repo_snapshot / snapshotTailWindow | 3 | 默认关，双开关 |
| 上传 | attachmentUpload / upload_target / shareArtifact | 5 | 加密制品 + 预签名 |

## 逐机制入口（file 级落点）

### 1. computer-use 浏览器自动化
- 引擎动作枚举：zcode.cjs grep `"navigate","back"`（46 动作）
- 前端权限面板：deobfuscated/preload/cuaPermissionPanel.cjs（cua 动作的 UI 权限）
- 内核工具：resources/tools/cua-helper/（computer-use 助手 + runtime-manifest.json）

### 2. 模型调用网关
- 端点：zcode.z.ai/api/v1/zcode-plan/anthropic（zai 内置计划，Anthropic Messages）
- 出口：zcode.cjs grep getModelConfigHeaders（11 处，注入 o11yHeaders）

### 3. MCP 协议
- 引擎：zcode.cjs grep callTool/listTools（6 处）
- SDK：reference/node_modules/@modelcontextprotocol/sdk/

### 4. 工具调度（agent 核心循环）
- zcode.cjs grep toolCall（1238）/ toolName（882）/ startTool（7）/ runTool（4）
- 这是 agent 每轮"选工具→执行→回传"的主循环

### 5. 遥测 / 可观测
- OpenTelemetry（otel trace）：grep opentelemetry（606）
- 阿里 ARMS RUM：grep arms（9）
- 数据大小遥测：zcode.cjs grep telemetry（153）

### 6. 仓库快照 + 上传
- 快照：grep repo_snapshot（3）/ snapshotTailWindow
- 双开关 AND 门：grep repoSnapshotIndexingEnabled && repoSnapshotIndexingUserConfigured
- 上传：grep attachmentUpload（5）/ upload_target / shareArtifact

### 7. 会话分享
- zcode.cjs grep conversationShareArtifactUploadDataSchema

## 全量产物地图（D:\dev\Zcode源码）

```
backend/zcode.cjs            引擎原版 3583 行（pnpm monorepo zcode-cli 构建）
main/(20) host/(15) preload/(6) scheduler/(1) metadata/(1)   原版
frontend/(4081)              原版 React 19 全量
reference/node_modules/(455) 第三方依赖 + package.json
deobfuscated/                ★ 全量解混淆（68M，js-beautify）：
   main/(20) host/(15) preload/(6+.pretty) scheduler/(1) renderer/(2686)
```

## 内核 native（符号级）

| 模块 | 文件 | 体积 | 说明 |
|---|---|---|---|
| node-pty | conpty.node / conpty_console_list.node / pty.node | 320K/144K/308K | Windows 终端 PTY（C/C++，第三方 prebuild） |
| ripgrep | tools/ripgrep/rg.exe | 5.2M | 代码搜索（Rust 二进制） |
| ugrep | tools/ugrep/ugrep.exe | 2.1M | 正则搜索（Rust 二进制） |
| cua-helper | tools/cua-helper/ | — | computer-use 助手（runtime-manifest.json） |

> native .node 均为第三方 C/C++ 预编译（node-pty），未做 IDA 级反汇编（超出本次范围）；
> rg/ugrep 为 Rust 官方二进制，非 ZCode 自研。

## 每条机制的触发条件 + 默认行为（源码实测字面值）

> 默认值按 zcode.cjs 里的 `default(!0)=true` / `default(!1)=false` 实测，非推断。

| 机制 | 触发条件 | 默认行为 | 源码锚点 |
|---|---|---|---|
| 仓库快照上传 | `repoSnapshotIndexingEnabled===true && repoSnapshotIndexingUserConfigured===true`（双开关 AND） | **默认关**（`default(!1)`） | grep `repoSnapshotIndexingEnabled===!0&&` |
| computer-use 入口 | 用户调用 cua 动作 | **入口默认隐藏**（`computerUseComposerEntryHidden default(!0)`=true） | grep `computerUseComposerEntryHidden` |
| 附件/制品下载上传 | share artifact 时选模式 | 默认 `local-download-upload`（本地暂存，不推远端），可选 `remote-download` | grep `local-download-upload` |
| 模型调用网关 | 每次 model request | 走 `zcode-plan/anthropic`（zai 内置计划），刷新前判 `shouldRefreshBeforeModelRequest` | grep `zcode-plan/anthropic` |
| 工具调度 | agent 每轮 loop | 必选工具（toolCall 1238 处为调用点密度），`startTool` 7 处为发起 | grep `toolCall`/`startTool` |
| MCP 协议 | 连外部 MCP server | `listTools` 17 / `callTool` 12，`convertToZCodeAgentMcpServer` 转换 | grep `listTools` |
| 遥测可观测 | 启动+事件 | OpenTelemetry trace（606 处）+ ARMS RUM（9 处）；布尔默认关略多（`default(!1)` 19 处 > `!0` 17 处） | grep `opentelemetry`/`arms` |

## 全默认值统计（引擎层）

`zcode.cjs` 里 `.default(!0)`（默认开）17 处 vs `.default(!1)`（默认关）19 处——
**隐私/上传类默认关占多**，印证整体设计是"能力默认保守，用户显式 opt-in 才启用"。
