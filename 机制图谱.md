# ZCode 机制图谱（v3.14.0 全量破解）

> 全部锚点可用 grep 复核。引擎 = backend/zcode.cjs（15438 行原版压缩）；
> 已解混淆全量产物在 deobfuscated/（2764 文件，含 main/host/preload/scheduler/renderer）。

## 机制命中统计（zcode.cjs 引擎内 grep 计数，v3.14.0）

| 机制 | 关键词 | 命中次数 | 说明 |
|---|---|---|---|
| 工具调度 | toolCall / toolName / startTool / runTool | 高密度 | agent 核心循环 |
| 遥测可观测 | opentelemetry / telemetry / o11y / arms | 51 处 | OpenTelemetry + ARMS |
| 模型调用网关 | getModelConfigHeaders / o11yHeaders | 11 | 模型出口 |
| MCP 协议 | callTool / listTools | 6 | @modelcontextprotocol |
| computer-use | computerUse / cua_ / playwright | 15 | 浏览器自动化 |
| 仓库快照 | repo_snapshot / snapshotTailWindow | 3 | 默认关，双开关 |
| 附件上传 | attachmentUpload / upload_target / shareArtifact | 5 | 加密制品 + 预签名 |
| **远程控制** | **webRemoteControl / relayWsUrl / relayProtocol** | **40+** | **v3.14 新增** |
| 设备指纹 | deviceMid / GetDeviceId / frameFingerprint | 7 | v3.14 强化 |

## 逐机制入口（file 级落点，v3.14.0）

### 1. 网页远程控制通道（v3.14.0 新增）
- main/index.js 搜 `webRemoteControlManager`（9 处）：WebSocket relay 管理器
- main/index.js 搜 `relayWsUrl` / `relayProtocol` / `relayMessageLogger`（40+ 处）
- 连接域：`cdn.zcode-ai.com` / `studio.zcode-ai.com`
- 认证：`deviceMid`（设备指纹）+ `X-Device-ID` header + auth
- 移动端：`mobileRemoteControlUrl` / `webRemoteControlExternalRelayDevice`
- 默认：需 `webRemoteControlExternalRelayDevice.deviceSid` 显式配才连

### 2. computer-use 浏览器自动化
- 引擎动作枚举：zcode.cjs grep `"navigate","back"`（46 动作）
- 前端权限面板：preload/cuaPermissionPanel.cjs
- 内核工具：resources/tools/cua-helper/

### 3. 模型调用网关
- 端点：zcode.z.ai/api/v1/zcode-plan/anthropic（zai 内置计划，Anthropic Messages）
- 出口：zcode.cjs grep `getModelConfigHeaders`（11 处，注入 o11yHeaders）

### 4. MCP 协议
- 引擎：zcode.cjs grep callTool/listTools（6 处）
- SDK：reference/node_modules/@modelcontextprotocol/sdk/

### 5. 工具调度（agent 核心循环）
- zcode.cjs grep toolCall / toolName / startTool / runTool

### 6. 遥测 / 可观测
- OpenTelemetry（otel trace）：grep opentelemetry
- 阿里 ARMS RUM：grep arms
- 数据大小遥测：grep telemetry（51 处）

### 7. 仓库快照 + 上传
- 快照：grep repo_snapshot / snapshotTailWindow
- 双开关 AND 门：grep `repoSnapshotIndexingEnabled===!0&&...UserConfigured===!0`
- 上传：grep attachmentUpload / upload_target / shareArtifact
- 详细源码见 `snippet/repo-snapshot-upload.md`

### 8. 会话分享
- zcode.cjs grep conversationShareArtifactUploadDataSchema

### 9. 自动更新
- main/index.js 搜 `updater disabled for this product flavor`（production 默认关）
- `autoDownloadAndInstallUpdates` 默认 false

### 10. 调度器 broker（v3.14 新增）
- scheduler/ 从 1 文件 → 3 文件（+broker 分片）
- 搜 `brokerExchange` / `callBrokerMethod` / `probeHelperHealth`
- host 新增 `tasksStorageWorker.js`（任务存储迁移 worker）

## 全量产物地图（D:\dev\Zcode源码，v3.14.0）

```
backend/zcode.cjs            引擎原版 15438 行 / 15M（pnpm monorepo zcode-cli）
main/(22) host/(18) preload/(7) scheduler/(3) metadata/(1)   原版
frontend/(4121)              原版 React 19 全量
reference/node_modules/(455) 第三方依赖 + package.json
deobfuscated/                ★ 全量解混淆（2764 文件）：
   main/(22) host/(18) preload/(7+.pretty) scheduler/(3) renderer/(2717)
```

## 内核 native（符号级）

| 模块 | 文件 | 体积 | 说明 |
|---|---|---|---|
| node-pty | conpty.node / conpty_console_list.node / pty.node | 320K/144K/308K | Windows 终端 PTY（C/C++，第三方 prebuild） |
| ripgrep | tools/ripgrep/rg.exe | 5.2M | 代码搜索（Rust 二进制） |
| ugrep | tools/ugrep/ugrep.exe | 2.1M | 正则搜索（Rust 二进制） |
| cua-helper | tools/cua-helper/ | — | computer-use 助手（runtime-manifest.json） |

## 默认值统计（引擎层 boolean）

`zcode.cjs`（v3.14.0）里 `.default(!0)`（默认开）vs `.default(!1)`（默认关）——
隐私/上传类默认关占多，印证整体设计是"能力默认保守，用户显式 opt-in 才启用"。

## 新增可疑面清单（v3.12.3 → v3.14.0）

| 项 | 旧版 | 新版 | 风险评估 |
|---|---|---|---|
| 远程控制通道 | 无 | **webRemoteControlManager + relay（40处）** | 新外部 WebSocket 出站面 + 设备指纹 |
| 设备指纹 | 弱 | 强化（deviceMid + frameFingerprint） | 上报 relay |
| 调度器 | 1 文件 | 3 文件（broker 分片） | 本地 helper RPC 面 |
| 任务存储 | 无 | tasksStorageWorker（host 层） | 数据迁移 worker |
| 自动更新 | 默认关 | 仍默认关（production flavor） | 无变化 |
| OSS 直连 | 无 | 仍无（aliyuncs 命中都是 dashscope LLM API） | 无新增 |

> **结论**：v3.14.0 无 RCE/恶意后门，但新增**官方远程控制通道**（手机遥控桌面 ZCode），
> 多了一个外部 WebSocket + 设备指纹出站面。要拦截：hosts 加 `cdn.zcode-ai.com` + `studio.zcode-ai.com`。
